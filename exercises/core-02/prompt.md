Directory: exercises/core-02

Pull and run the official `nginx` image, expose port 80, and verify you can access it locally.

1. Run an `nginx` container.
2. Map host port `8080` to container port `80`.
3. Use `curl http://localhost:8080` to verify it's working.

